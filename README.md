## 题目：简单Web服务器

### 一）设计内容

编写一个简单的多线程Web服务器程序，能够监听指定端口，接受客户端的HTTP请求，解析请求报文，从本地文件系统读取请求的资源文件，并构造HTTP响应报文返回给客户端。支持GET方法，能够正确返回状态码200
OK以及404 Not Found等响应。

### 二）设计要求

1) 设计一个简单的控制界面，可以配置服务器监听的端口号，启动和停止服务器，并实时显示访问日志（包括客户端IP、请求的URL、响应状态码等）。
2) 实现多线程处理并发请求，每个客户端连接由一个独立线程处理，避免串行服务。
3) 解析HTTP请求行和头部，获取请求方法、URL和HTTP版本。若请求方法不是GET，则返回501 Not Implemented状态码。
4) 根据请求的URL（如/index.html）在服务器指定的根目录下查找对应文件。若文件存在，读取文件内容，构造包含状态行、Content-Type、Content-Length等头部的响应报文返回；若文件不存在，返回404
   Not Found页面（可自定义一个简单的HTML错误页面）。
5) 编程语言不限。